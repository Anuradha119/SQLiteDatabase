# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SQLiteDatabase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SQLiteDatabase

  target 'SQLiteDatabaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SQLiteDatabaseUITests' do
    # Pods for testing
  end
pod 'SQLite.swift', '~> 0.12.0'
end
